pipeline {
    agent any

    stages {
        stage('setup') {
            steps {
                echo 'First step...'
                sh 'ls'
                sh 'ls -la software/installer'
            }
        }
    }
}